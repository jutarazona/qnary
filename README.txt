#first clone de repo
#then run the next command
npm ci for istall dependencies
#For running test just run the command
npm run test
#The test result will be saved on the directory html-report, open the report.html on the navigator to see the web view,
#you could extract the data info in results.js